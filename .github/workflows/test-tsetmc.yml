name: Test Alternative API

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test alternative API
        run: |
          echo "Testing alternative API..."
          curl -L --max-time 30 \
            -A "Mozilla/5.0" \
            -o response.json \
            "https://api.azhir365.com/v1/bourse/?token=e193b1613082a46116b95f4e12b6cedd&type=close_price&name=فملی"

          echo "Response:"
          cat response.json
